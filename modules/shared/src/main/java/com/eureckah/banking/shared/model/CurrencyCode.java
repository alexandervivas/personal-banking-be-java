package com.eureckah.banking.shared.model;

/**
 * Currency codes supported by the system for accounts and transactions. Keep minimal set for now;
 * extend when new currencies are supported.
 */
public enum CurrencyCode {
    USD,
    EUR,
    COP
}
